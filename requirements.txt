# HierTable-RAG Dependencies
# Python 3.10+ required

pdfplumber>=0.9.0        # PDF 테이블 추출

img2table>=1.2.0         # 이미지 테이블 인식

pandas>=2.0.0

beautifulsoup4>=4.12.0   # HTML 파싱

lxml>=4.9.0

networkx>=3.1            # 그래프 구조

langchain>=0.1.0         # LLM 프레임워크

openai>=1.0.0

faiss-cpu>=1.7.4         # 벡터 검색

sentence-transformers>=2.2.0
transformers>=4.30.0       # For RoBERTa/BERT semantic encoding
torch>=2.0.0               # PyTorch for deep learning
scipy>=1.10.0              # For Hungarian algorithm in GraphOptimizer

pytesseract>=0.3.10      # OCR (필요시)
apted>=1.0.3             # TEDS calculation
rouge-score>=0.1.2       # RAG evaluation

# GNN-CSP TSR Dependencies
torch-geometric>=2.3.0   # Graph Neural Networks
ortools>=9.5.2237        # Constraint solving (CP-SAT solver)

# Diagnosis Module Dependencies
pillow>=9.0.0            # Image processing for overlays
opencv-python>=4.5.0     # Advanced drawing
scikit-learn>=1.0.0      # For bootstrap sampling
tqdm>=4.65.0             # Progress bars

# External TSR Baseline Engines (added for diagnosis module)
paddleocr>=2.7.0         # PaddleOCR for noisy OCR source  
paddlepaddle-gpu>=2.5.0  # PaddlePaddle GPU (or use paddlepaddle for CPU)
paddlex>=3.0.0           # PaddleX for SLANet_plus TSR
docling>=1.0.0           # Docling (IBM) for PDF/image TSR
