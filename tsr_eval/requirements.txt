# TSR Evaluation Requirements
# Install with: pip install -r tsr_eval/requirements.txt

# ── Core ────────────────────────────────────────────────────────────────────
numpy>=1.24
Pillow>=9.0
opencv-python-headless>=4.8
matplotlib>=3.7
pandas>=2.0
pyyaml>=6.0

# ── Table Transformer (TATR) ────────────────────────────────────────────────
torch>=2.0
transformers>=4.35
timm>=0.9           # required by table-transformer

# ── PaddleOCR / PaddleX ─────────────────────────────────────────────────────
# CPU-only PaddlePaddle:
#   pip install paddlepaddle -f https://www.paddlepaddle.org.cn/whl/linux/mkl/avx/stable.html
# GPU:
#   pip install paddlepaddle-gpu  (match CUDA version)
paddleocr>=2.7
# paddlex>=3.0.0     # optional: required only for SLANeXt_wired/wireless
beautifulsoup4>=4.12
lxml>=4.9

# ── Tesseract ───────────────────────────────────────────────────────────────
pytesseract>=0.3.10
# System Tesseract binary must be installed separately:
#   Ubuntu/Debian: sudo apt-get install tesseract-ocr tesseract-ocr-eng
#   macOS:         brew install tesseract

# ── GT Metrics ──────────────────────────────────────────────────────────────
# TEDS (optional, provides accurate TEDS if installed):
#   pip install teds           (https://github.com/ibm-aur-nlp/PubTabNet)
