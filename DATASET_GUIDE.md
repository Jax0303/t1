# 데이터셋 가이드

## 데이터셋 구조

### 1. 149.표 정보 질의 응답 데이터

```
data/raw/dataset1/149.표 정보 질의 응답 데이터/
├── 01-1.정식개방데이터/
│   ├── Training/
│   │   ├── 01.원천데이터/TS.zip
│   │   └── 02.라벨링데이터/TL.zip
│   └── Validation/
│       ├── 01.원천데이터/VS.zip
│       └── 02.라벨링데이터/VL.zip
```

**원천데이터 (TS.zip, VS.zip)**
- JSON 형식
- 문서 정보, 문단, 표 등 포함
- 구조: `{"Dataset": "...", "data": [...]}`

**라벨링데이터 (TL.zip, VL.zip)**
- JSON 형식
- 질문-답변 쌍 및 표 라벨 포함
- 표 정보가 구조화되어 있음

### 2. 개정 표준취업규칙 (다중 형식)

```
data/raw/dataset2/
├── 개정 표준취업규칙(2025년, 배포).hwp
├── 개정 표준취업규칙hwpx(2025년, 배포).hwpx
└── 개정+표준취업규칙(2025년,+배포).hwp.pdf
```

**특징**
- 같은 내용의 문서가 3가지 형식으로 제공
- 형식별 표 추출 성능 비교 가능
- 파일명 기반으로 자동 매칭

## 데이터 처리

### 자동 처리

데이터셋 로더가 자동으로:
1. 압축 파일 해제
2. JSON 파일 파싱
3. 파일 형식 인식
4. 파일 매칭

### 수동 처리

필요시 다음 스크립트로 데이터 준비:
```bash
python setup_data_dirs.py
```

## 데이터 형식 변환

### 라벨 데이터 변환

라벨링 데이터는 다양한 형식으로 저장될 수 있습니다:

1. **2D 배열 형식**
```json
{
  "data": [
    ["헤더1", "헤더2"],
    ["값1", "값2"]
  ]
}
```

2. **행 단위 형식**
```json
{
  "rows": [
    {"col1": "값1", "col2": "값2"}
  ]
}
```

3. **셀 단위 형식**
```json
{
  "cells": [
    {"row": 0, "col": 0, "value": "값1"}
  ]
}
```

모든 형식이 자동으로 DataFrame으로 변환됩니다.

## 파일 매칭

### 다중 형식 파일 매칭

같은 내용의 파일들은 파일명 기반으로 자동 매칭됩니다:
- 괄호 내용 제거
- 확장자 관련 키워드 제거
- 공백 정리
- 공통 패턴 추출

예: 
- `개정 표준취업규칙(2025년, 배포).hwp`
- `개정 표준취업규칙hwpx(2025년, 배포).hwpx`
- `개정+표준취업규칙(2025년,+배포).hwp.pdf`

→ 모두 "개정 표준취업규칙"으로 매칭

### 라벨-파일 매칭

라벨링 데이터와 파일은 다음 방법으로 매칭:
1. 파일명에서 숫자 ID 추출
2. 라벨 키와 비교
3. 부분 일치 검색

## 문제 해결

### 파일을 찾을 수 없음
- `data/raw/` 디렉토리 확인
- 압축 파일이 올바르게 해제되었는지 확인

### 인코딩 문제
- 파일명에 특수문자가 있는 경우 자동 처리
- 필요시 파일명 변경

### 매칭 실패
- 파일명 패턴 확인
- 수동으로 매칭 정보 제공 가능

