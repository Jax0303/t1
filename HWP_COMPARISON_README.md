# HWP 직접 파싱 vs HWPX 변환 비교 실험 가이드

## 개요

이 실험은 HWP 파일을 RAG 시스템에서 바로 활용하기 위한 새로운 파싱/정규화 방법을 설계하고, 기존의 HWPX 변환 기반 베이스라인과 정량적으로 비교하여 성능 개선을 입증합니다.

## 실험 목표

1. **HWP 직접 파싱 방법 설계**: HWP 파일을 HWPX로 변환하지 않고 직접 파싱하는 방법 구현
2. **표 추출 성능 비교**: 두 방법의 표 추출 정확도 및 처리 속도 비교
3. **RAG 성능 비교**: EM, F1, Hit@K 지표를 사용한 질의응답 성능 비교
4. **성능 개선 입증**: 새로운 방법이 베이스라인 대비 얼마나 개선되었는지 정량적 입증

## 구현된 방법

### 1. HWP 직접 파싱 방법 (새로운 방법)

다음 세 가지 방법을 순차적으로 시도:

1. **pyhwp 라이브러리 사용** (HWP5 형식)
   - HWP5 파일의 구조화된 데이터 직접 파싱
   - 표 정보를 레코드 단위로 추출

2. **olefile 라이브러리 사용** (OLE2 구조)
   - OLE2 기반 HWP 파일의 스트림 파싱
   - 본문 스트림에서 표 정보 추출

3. **바이너리 직접 파싱** (대체 방법)
   - 파일을 바이너리로 읽어 텍스트 패턴 매칭
   - 탭/공백으로 구분된 표 형태 데이터 추출

**정규화 강화**:
- 텍스트 정규화 (공백, 줄바꿈, 특수문자 처리)
- 표 구조 보존 (헤더 인식, 셀 정렬)
- RAG 최적화 (임베딩에 적합한 형식으로 변환)

### 2. HWPX 변환 기반 방법 (베이스라인)

1. **HWPX 직접 파싱**: 이미 변환된 HWPX 파일 파싱
2. **HWP -> HWPX 변환 후 파싱**: HWP 파일을 HWPX로 변환 후 파싱

## 평가 지표

### 표 추출 성능
- **표 개수**: 추출된 표의 개수 비교
- **추출 시간**: 파일당 평균 처리 시간
- **F1-score**: 표 내용 유사도 (직접 파싱 vs HWPX 변환)

### RAG 성능
- **EM Score (Exact Match)**: 정답과 완전히 일치하는 답변의 비율
- **F1 Score**: 답변의 정밀도와 재현율의 조화 평균
- **Hit@K**: 상위 K개 검색 결과 중 관련 문서가 포함된 비율
  - Hit@1: 상위 1개 결과에 정답 포함 여부
  - Hit@3: 상위 3개 결과에 정답 포함 여부
  - Hit@5: 상위 5개 결과에 정답 포함 여부
- **BLEU Score**: 답변의 품질 평가 (n-gram 기반)

## 사용 방법

### 1. 필요한 라이브러리 설치 (선택적)

HWP 직접 파싱을 위해 다음 라이브러리를 설치할 수 있습니다:

```bash
# pyhwp 설치 (HWP5 파일 파싱용)
pip install pyhwp

# olefile 설치 (OLE2 파일 파싱용)
pip install olefile
```

**참고**: 이 라이브러리들이 없어도 바이너리 파싱 방법이 작동합니다.

### 2. 데이터 준비

`data/raw/dataset2/` 디렉토리에 다음 파일들이 있어야 합니다:
- `*.hwp`: HWP 파일
- `*.hwpx`: HWPX 파일 (같은 내용, 비교용)

### 3. 쿼리 준비

`sample_queries.json` 파일이 있으면 자동으로 로드됩니다.
없으면 기본 쿼리가 생성됩니다.

쿼리 형식:
```json
[
  {
    "question": "표의 첫 번째 행 정보를 알려주세요",
    "table_id": "table_001",
    "expected_info": {
      "columns": ["열1", "열2"],
      "sample_values": ["값1", "값2"]
    }
  }
]
```

### 4. 실험 실행

```bash
python run_hwp_comparison.py
```

### 5. 결과 확인

실험 결과는 다음 파일에 저장됩니다:
- `results_hwp_comparison.json`: 상세 결과 데이터

콘솔에 출력되는 리포트에서:
- 표 추출 성능 비교
- RAG 성능 비교 (EM, F1, Hit@K)
- 성능 개선 효과 (베이스라인 대비 개선율)

## 결과 해석

### 성능 개선 예시

```
[성능 개선 분석]
베이스라인: HWPX 직접 파싱

[HWP 직접 파싱] vs [HWPX 직접 파싱]
  EM Score: 0.8500 (+0.1000, +13.3%)
  F1 Score: 0.8200 (+0.0800, +10.8%)
  Hit@1: 0.7500 (+0.0500, +7.1%)
  Hit@3: 0.9000 (+0.1000, +12.5%)
  Hit@5: 0.9500 (+0.0500, +5.6%)
```

이 결과는 HWP 직접 파싱 방법이 HWPX 변환 방법보다:
- EM Score가 13.3% 향상
- F1 Score가 10.8% 향상
- Hit@K 지표도 전반적으로 향상

## 기술적 세부사항

### HWP 직접 파싱의 장점

1. **변환 과정 제거**: HWPX 변환 단계가 없어 처리 속도 향상
2. **정보 손실 최소화**: 변환 과정에서 발생할 수 있는 정보 손실 방지
3. **정규화 강화**: RAG에 최적화된 텍스트 정규화 적용
4. **표 구조 보존**: 병합 셀, 서식 정보 등 표 구조 정보 보존

### 정규화 방법

1. **텍스트 정규화**:
   - 연속된 공백 제거
   - 특수 문자 정리 (제어 문자 제거)
   - 줄바꿈 정리

2. **표 구조 정규화**:
   - 행 길이 맞추기 (빈 셀 추가)
   - 헤더 자동 인식
   - 셀 내용 정규화

3. **RAG 최적화**:
   - 임베딩에 적합한 형식으로 변환
   - 구조 정보 보존 (행/열 정보 포함)

## 문제 해결

### pyhwp 설치 오류

```bash
# 대안: olefile 사용
pip install olefile

# 또는 바이너리 파싱 방법 사용 (기본 제공)
```

### HWPX 파일이 없는 경우

HWPX 파일이 없어도 HWP 직접 파싱 방법은 작동합니다.
HWP->HWPX 변환 방법만 건너뜁니다.

### API 키 오류

`config.yaml`에 올바른 API 키가 설정되어 있는지 확인하세요:
- OpenAI: `api.openai.api_key`
- Gemini: `api.gemini.api_key`

## 참고

- HWP 파일 형식은 복잡하므로 모든 파일에서 완벽한 파싱이 보장되지 않을 수 있습니다.
- 실제 성능은 파일의 구조와 내용에 따라 달라질 수 있습니다.
- pyhwp나 olefile이 설치되지 않은 경우, 바이너리 파싱 방법이 자동으로 사용됩니다.

