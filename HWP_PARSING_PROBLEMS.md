# HWP 파싱 문제 인식

## 핵심 문제 요약

### 현재 상황
- **HWPX 파싱**: ✅ 43개 표 성공적으로 추출
- **HWP 직접 파싱**: ❌ 1개만 추출 (데이터 깨짐)
- **성공률**: 0% (실제로는 실패)

---

## 문제 1: 레코드 구조 파싱 실패

### 증상
- Section1 스트림(212KB)에서 유효한 레코드를 찾지 못함
- 레코드 파싱 시도 시 대부분 무효한 시도로 판단됨

### 원인 분석

#### 1.1 레코드 헤더 구조 불명확
```
현재 가정: 4바이트 태그 + 4바이트 크기
실제: HWP 버전별로 레코드 구조가 다를 수 있음
```

**문제점:**
- HWP 5.0 레코드 구조를 정확히 모름
- 레코드 정렬(alignment)을 고려하지 않음
- 레코드 헤더에 추가 필드가 있을 수 있음

#### 1.2 레코드 식별 실패
```python
# 현재 코드
tag = struct.unpack('<I', data[pos:pos+4])[0]
size = struct.unpack('<I', data[pos+4:pos+8])[0]

# 문제: tag와 size 값이 유효한지 판단하는 기준이 부족
if size > len(data) or size < 0:  # 너무 단순한 검증
    pos += 1
    continue
```

**문제점:**
- 레코드 태그 패턴을 모름 (표 관련 태그가 무엇인지 불명확)
- 레코드 크기 유효성 검사가 부족함
- 레코드 간격/정렬을 고려하지 않음

#### 1.3 비효율적인 탐색
```python
# 현재 코드
except Exception:
    pos += 1  # 실패 시 1바이트씩 이동
```

**문제점:**
- 레코드 파싱 실패 시 1바이트씩 이동하여 비효율적
- 레코드 시작 위치를 찾는 더 나은 방법이 필요

---

## 문제 2: 압축 해제 실패

### 증상
- Section1 스트림이 압축되어 있을 가능성이 있으나 해제 실패
- zlib.decompress() 시도 시 예외 발생

### 원인 분석

#### 2.1 압축 형식 불명확
```python
# 현재 코드
try:
    decompressed = zlib.decompress(stream_data)
except:
    pass  # 압축이 아니라고 가정
```

**문제점:**
- zlib 압축의 wbits 옵션을 시도하지 않음
- 다른 압축 형식(예: LZ77)을 고려하지 않음
- 압축 여부를 사전에 확인하지 않음

#### 2.2 압축 헤더 확인 부재
- 압축된 데이터는 보통 헤더를 가지고 있음
- 현재 코드는 헤더를 확인하지 않고 바로 decompress 시도

---

## 문제 3: 표 데이터 추출 실패

### 증상
- 레코드에서 표를 찾지 못함
- 텍스트 패턴 매칭으로 폴백하지만 실패

### 원인 분석

#### 3.1 표 레코드 식별 불가
```python
# 현재 코드
# 표 관련 태그인지 확인 (일반적인 HWP 태그 값)
# 실제 태그 값은 HWP 버전에 따라 다를 수 있음
```

**문제점:**
- 표 관련 태그 값을 정확히 모름
- HWP 버전별 태그 매핑이 없음
- 표 시작/끝 레코드를 식별하지 못함

#### 3.2 셀 데이터 파싱 실패
```python
# 현재 코드
for encoding in ['utf-16-le', 'utf-8', 'cp949']:
    try:
        text = record_data.decode(encoding, errors='ignore')
        break
    except:
        continue
```

**문제점:**
- 인코딩 추측에 의존
- 셀 데이터 구조를 모름 (텍스트만 있는지, 포맷 정보도 있는지)
- 제어 문자나 바이너리 데이터 처리 부족

#### 3.3 표 구조 파싱 실패
- 행/열 구조를 정확히 파싱하지 못함
- 병합된 셀, 빈 셀 등을 처리하지 못함

---

## 문제 4: 디버깅 및 로깅 부족

### 증상
- 파싱 실패 원인을 추적하기 어려움
- 어떤 레코드를 찾았는지 알 수 없음

### 원인 분석

#### 4.1 로깅 부재
```python
# 현재 코드
except Exception:
    pass  # 조용히 실패
```

**문제점:**
- 예외가 발생해도 로그가 없음
- 디버깅 정보가 전혀 없음
- 실패 지점을 찾을 수 없음

#### 4.2 통계 정보 부족
- 몇 개의 레코드를 찾았는지 모름
- 어떤 태그를 발견했는지 모름
- 파싱 진행 상황을 알 수 없음

---

## 문제 5: HWP 파일 포맷 스펙 부재

### 근본 원인
- **HWP 파일 포맷이 공개되지 않음**
- 한컴오피스에서 공식 스펙을 제공하지 않음
- 역공학에 의존해야 함

### 영향
- 레코드 구조를 추측해야 함
- 태그 값을 실험적으로 찾아야 함
- 버전별 차이를 직접 확인해야 함

---

## 문제 6: 현재 구현의 한계

### 6.1 단순한 접근 방식
```python
# 현재: 바이너리 데이터 → 텍스트 변환 → 패턴 매칭
# 문제: HWP는 구조화된 바이너리 포맷이므로 이 방식으로는 한계가 있음
```

### 6.2 라이브러리 활용 부족
- `pyhwp` 라이브러리가 있으나 사용하지 않음
- 공식 파싱 도구를 활용하지 않음

### 6.3 하이브리드 접근 부재
- HWPX 변환을 자동으로 시도하지 않음
- 직접 파싱 실패 시 대안이 없음

---

## 문제 우선순위

### 🔴 Critical (즉시 해결 필요)
1. **레코드 구조 파싱 실패** - 근본 원인
2. **압축 해제 실패** - 데이터 접근 불가

### 🟡 High (중요)
3. **표 데이터 추출 실패** - 목표 달성 불가
4. **디버깅 정보 부족** - 문제 해결 어려움

### 🟢 Medium (개선 필요)
5. **HWP 포맷 스펙 부재** - 장기적 문제
6. **라이브러리 활용 부족** - 효율성 문제

---

## 해결 방향

### 단기 해결책
1. ✅ pyhwp 라이브러리 활용
2. ✅ 압축 해제 로직 개선
3. ✅ 로깅 추가

### 중기 해결책
1. ⚠️ HWP 레코드 구조 역공학
2. ⚠️ 표 태그 매핑 구축
3. ⚠️ HWPX 변환 자동화

### 장기 해결책
1. 🔄 한컴오피스 SDK 활용
2. 🔄 머신러닝 기반 구조 인식
3. 🔄 공식 스펙 확보 (가능한 경우)

---

## 결론

**핵심 문제**: HWP 파일의 레코드 구조를 정확히 파싱하지 못하여 표 데이터에 접근할 수 없음

**근본 원인**: 
1. HWP 파일 포맷 스펙 부재
2. 레코드 구조 파싱 로직 부정확
3. 압축 해제 실패

**해결 전략**:
1. pyhwp 같은 검증된 라이브러리 활용
2. HWPX 변환을 통한 우회
3. 레코드 구조 역공학 및 개선

